# Dependencies
node_modules/

# Build output (Vue render bundle + web app + single-file static viewer)
dist/
dist-render/
dist-web-single/

# Render artifacts
*.mp4
*.pdf
.artifacts/
.worktrees/
frames/
*-frames/
audio/
*.mp3
*.png

# OS / editor cruft
.DS_Store
*.swp
*~

# Kitsoki local runtime
.kitsoki.local.yaml
.kitsoki/sessions/
.artifacts/
.context
.worktrees

# Local npm registry config (do not commit — breaks CI / off-VPN installs)
.npmrc

# Local Codex MCP config (machine-specific cwd — do not commit, breaks CI)
.codex/
